<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <title>Attendance Report</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            font-size: 12px;
            color: #333;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
            border-bottom: 2px solid #333;
            padding-bottom: 10px;
        }
        .header h1 {
            margin: 0;
            font-size: 24px;
        }
        .header p {
            margin: 5px 0 0 0;
            color: #666;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 20px;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f4f4f4;
            font-weight: bold;
        }
        .text-center {
            text-align: center;
        }
        .status-present { color: green; }
        .status-absent { color: red; }
        .status-late { color: orange; }
        .status-leave { color: blue; }
        .status-shift { color: #0284c7; font-weight: bold; }
        .footer {
            margin-top: 40px;
            font-size: 10px;
            text-align: center;
            color: #999;
        }
    </style>
</head>
<body>
    <div class="header">
        <h1>Attendance Report</h1>
        <p>Generated on: {{ now()->format('d M Y, H:i') }}</p>
    </div>

    <table>
        <thead>
            <tr>
                <th>No</th>
                <th>Employee</th>
                <th>Date</th>
                <th>Status</th>
                <th>Check-In</th>
                <th>Check-Out</th>
                <th>Work (Mins)</th>
                <th>Late (Mins)</th>
            </tr>
        </thead>
        <tbody>
            @forelse($attendances as $index => $attendance)
                <tr>
                    <td class="text-center">{{ $index + 1 }}</td>
                    <td>
                        {{ $attendance->employee->full_name ?? '' }}<br>
                        <small style="color:#666">{{ $attendance->employee->employee_no ?? '' }}</small>
                    </td>
                    <td>{{ \Carbon\Carbon::parse($attendance->attendance_date)->format('d M Y') }}</td>
                    <td>
                        @php
                            $statusDisplay = ucfirst($attendance->status);
                            $statusClass = 'status-' . strtolower($attendance->status);
                            $isToday = \Carbon\Carbon::parse($attendance->attendance_date)->isToday();
                            
                            if (in_array(strtolower($attendance->status), ['absent', 'alpha']) && $isToday) {
                                $shift = $attendance->employeeSchedule?->shift;
                                if ($shift && !empty($shift->start_time)) {
                                    $shiftStart = \Carbon\Carbon::parse($attendance->attendance_date . ' ' . $shift->start_time, 'Asia/Jakarta');
                                    if (\Carbon\Carbon::now('Asia/Jakarta')->lessThan($shiftStart)) {
                                        $statusDisplay = ($shift->name ?? ($shift->code ?? 'Shift')) . ' (' . substr($shift->start_time, 0, 5) . ')';
                                        $statusClass = 'status-shift';
                                    }
                                }
                            }
                        @endphp
                        <span class="{{ $statusClass }}">
                            {{ $statusDisplay }}
                        </span>
                    </td>
                    <td>{{ $attendance->checkin_at ? \Carbon\Carbon::parse($attendance->checkin_at)->format('H:i') : '-' }}</td>
                    <td>{{ $attendance->checkout_at ? \Carbon\Carbon::parse($attendance->checkout_at)->format('H:i') : '-' }}</td>
                    <td class="text-center">{{ $attendance->work_duration_minutes ?? '-' }}</td>
                    <td class="text-center">{{ $attendance->late_minutes ?? '-' }}</td>
                </tr>
            @empty
                <tr>
                    <td colspan="8" class="text-center">No attendance records found.</td>
                </tr>
            @endforelse
        </tbody>
    </table>

    <div class="footer">
        Generated by HR Management System
    </div>
</body>
</html>
